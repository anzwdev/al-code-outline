# Command line runner for AL Language Server

This command line runner is used to run AL Language Server in a separate process. It is used by AL Language Extension for Visual Studio Code to run the language server in a separate process.
